import 'package:flutter/material.dart';

class UserCenterIndex extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('用户中心'),
    );
  }
}
